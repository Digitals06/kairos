// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BenchmarkVariant } from "./BenchmarkVariant";
import type { RankTier } from "./RankTier";
import type { SnapshotPoint } from "./SnapshotPoint";

/**
 * One benchmark row on the overview grid.
 */
export type BenchmarkCard = { benchmark_id: number, benchmark_name: string, abbreviation: string, difficulty_name: string, rank: RankTier | null, benchmark_progress: number, next_rank_name: string | null, next_rank_delta: number | null, avg_score: number, high_score: number, avg_improvement_pct: number | null, high_improvement_pct: number | null, samples: number, last_synced: string | null, 
/**
 * Whether this benchmark is pinned to the top of the player's list.
 */
is_favorite: boolean, 
/**
 * Full snapshot history so the UI can draw sparklines without an N+1 of
 * per-card detail calls (those starve the main thread on 70-card grids).
 */
snapshot_history: Array<SnapshotPoint>, 
/**
 * evxl-style benchmark types (Clicking, Tracking, …) from the registry's
 * categories/subcategories — powers the overview type filter.
 */
benchmark_types: Array<string>, 
/**
 * The benchmark's single style (Static, Dynamic, Micro, …) when it is
 * purely that style — style filter tabs only match pure benchmarks.
 * None for multi-style benchmarks (they only match family tabs).
 */
pure_type: string | null, 
/**
 * Overview family fold: how many played difficulties this card aggregates
 * (1 = single difficulty; >1 = the rank shown is the family's best).
 */
difficulty_count: number, 
/**
 * All played difficulties of the family, best-first; the UI unrolls
 * these inside the card. Populated when difficulty_count > 1.
 */
variants: Array<BenchmarkVariant>, };
