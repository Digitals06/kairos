// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BenchmarkCard } from "./BenchmarkCard";
import type { CategoryCard } from "./CategoryCard";
import type { Metrics } from "./Metrics";
import type { PlayPoint } from "./PlayPoint";
import type { RankTier } from "./RankTier";
import type { ScenarioHistorySeries } from "./ScenarioHistorySeries";
import type { ScenarioRank } from "./ScenarioRank";
import type { SnapshotPoint } from "./SnapshotPoint";

/**
 * Full detail for one benchmark: card + snapshot history + CSV plays +
 * scenario tiers + per-category progress from the newest snapshot.
 */
export type BenchmarkDetail = { card: BenchmarkCard, snapshot_history: Array<SnapshotPoint>, plays: Array<PlayPoint>, scenario_ranks: Array<ScenarioRank>, categories: Array<CategoryCard>, 
/**
 * Per-scenario score history across snapshots (scenario-scoped trends).
 */
scenario_history: Array<ScenarioHistorySeries>, 
/**
 * The difficulty's rank ladder (name + color, worst → best) for the
 * evxl-style per-tier threshold matrix.
 */
rank_tiers: Array<RankTier>, 
/**
 * Per-scenario improvement metrics (avg/high/improvement from CSV plays),
 * keyed by scenario name — powers the stat cards when a scenario is
 * selected in the chart picker.
 */
scenario_metrics: { [key in string]: Metrics }, };
